name: reg-materiality-filter
model: claude-opus-4-7
system:
  text: |
    あなたは feed-reader からの検証済みフィードアイテムリストを受け取ります。
    デプロイチームのウォッチリストと重要性閾値(ディスク上の regulatory-legal
    設定から読み込み ― 監視中の規制当局、ポリシーエリアタグ、「always
    material / review-worthy / FYI」ティア)に対して各アイテムを分類します。

    各アイテムについて、ちょうど 1 つの分類を発行してください:
      - `material`       — 「always material」のバーを満たす。アクションが必要な可能性が高い
      - `informational` — レビュー価値あり。評価して判断する
      - `skip`          — 閾値未満。ダイジェストからドロップ

    あなたは純粋な計算です。MCP なし、Web なし、Write なし。渡された構造化
    JSON とディスク上の設定ファイルを読みます。スキーマ検証済みの JSON のみを
    返してください。

tools:
  - type: agent_toolset_20260401
    default_config: { enabled: false }
    configs:
      - { name: read, enabled: true }
      - { name: grep, enabled: true }
      - { name: glob, enabled: true }

mcp_servers: []
skills: []
callable_agents: []

output_schema:
  type: object
  required: [as_of, items]
  additionalProperties: false
  properties:
    as_of: { type: string, maxLength: 10, pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" }
    items:
      type: array
      maxItems: 500
      items:
        type: object
        additionalProperties: false
        required: [source, title, date, url, classification, rationale]
        properties:
          source:         { type: string, maxLength: 64,  pattern: "^[A-Za-z0-9 ._/-]+$" }
          title:          { type: string, maxLength: 400 }
          date:           { type: string, maxLength: 10,  pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}$" }
          agency:         { type: string, maxLength: 120, pattern: "^[A-Za-z0-9 .,&()_/-]+$" }
          summary:        { type: string, maxLength: 1000 }
          url:            { type: string, maxLength: 512 }
          docket_id:      { type: string, maxLength: 64,  pattern: "^[A-Za-z0-9 ._/:-]+$" }
          classification: { type: string, maxLength: 16,  pattern: "^(material|informational|skip)$" }
          rationale:      { type: string, maxLength: 300 }
          policy_areas_touched:
            type: array
            maxItems: 20
            items: { type: string, maxLength: 64, pattern: "^[A-Za-z0-9 _-]+$" }
